# Copy to frontend/.env for local development.
VITE_API_URL=http://localhost:8000
# Local demo only: shows the "Quick Demo Login" panel (with the demo password). NEVER set this on a public deployment.
VITE_SHOW_DEMO_LOGIN=true
